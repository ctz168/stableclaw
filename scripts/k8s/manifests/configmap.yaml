apiVersion: v1
kind: ConfigMap
metadata:
  name: stableclaw-config
  labels:
    app: stableclaw
data:
  stableclaw.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.stableclaw/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "StableClaw Assistant",
            "workspace": "~/.stableclaw/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # StableClaw Assistant

    You are a helpful AI assistant running in Kubernetes.
