# stableclaw npm 发布注意事项

> 本文档记录了手动发布 stableclaw 到 npm 时必须遵守的流程和踩过的坑。
> 违反其中任何一条都可能导致发布的包在生产环境无法运行。

> ⛔⛔⛔ **最高优先级提醒**：
>
> 1. `prepublishOnly` 已配置为自动运行 `pnpm build && pnpm ui:build`，
> 所以直接 `npm publish` 时会自动完成完整构建。但如果你用 `--skip-build` 或手动构建，
> **必须同时跑两条命令**，缺一不可！UI 构建是最容易忘记的一步。
>
> 2. **绝对禁止使用 `npm publish --ignore-scripts`**！这会跳过 `prepublishOnly`，
> 导致 UI 构建产物可能缺失。如果必须跳过构建（如 CI 预构建），发布前必须验证：
> `ls dist/control-ui/index.html` 和 `npm pack --dry-run | rg control-ui`。
>
> 3. `scripts/ui.js` 已支持 npm/npx 作为 pnpm 的 fallback（2026.5.3），
> 不再强制要求 pnpm 环境。

## 一、发布前置条件

| 条件 | 要求 |
|------|------|
| Node.js | >= 22.12 |
| pnpm | 必须在 PATH 中可调用（含 corepack shims 环境） |
| npm token | 需要有 `stableclaw` 包的发布权限（Automation 类型） |
| 分支 | 建议从 `main` 分支发布 |

## 二、发布流程（必须按顺序执行）

### 步骤 1：Bump 版本号

编辑 `package.json`，修改 `version` 字段：

```bash
# 项目使用 calver 日期版本号格式：YYYY.M.MINOR
# 例如 2026.4.9 → 2026.4.10
```

同时运行 `node scripts/build-stamp.mjs` 同步构建时间戳。

### 步骤 2：安装依赖

```bash
pnpm install
```

### 步骤 3：执行完整构建（最关键的一步）

**方式 A：直接 `npm publish`（推荐，自动构建）**

`package.json` 中的 `prepublishOnly` 脚本已配置为：
```json
"prepublishOnly": "pnpm build && pnpm ui:build"
```
这意味着直接运行 `npm publish` 时，npm 会**自动**先执行 `pnpm build` 和 `pnpm ui:build`。
不需要手动分步构建。

**方式 B：手动构建（仅在跳过 prepublishOnly 时需要）**

如果你需要跳过自动构建（如用 `--skip-build` 参数或手动控制流程），**必须依次执行两条命令，缺一不可**：

```bash
pnpm build      # 核心 TypeScript 编译
pnpm ui:build   # Dashboard 控制面板 UI 构建
```

> ⛔ **注意：`pnpm build` 不包含 `pnpm ui:build`！它们是两条独立的命令。**
> 如果只跑 `pnpm build` 而不跑 `pnpm ui:build`，发布的包打开 Dashboard 时会报错：
> `Control UI assets not found.`

**绝对不能跳过任何一条。** `pnpm build` 会调用 tsdown 编译整个项目，生成以下关键文件：

- `dist/entry.js` — CLI 入口文件
- `dist/index.js` — 主模块
- `dist/extensions/*/runtime-api.js` — 各扩展的运行时接口
- `dist/plugin-sdk/*.js` — 插件 SDK 导出
- `dist/cli-startup-metadata.json` — CLI 启动元数据

`pnpm ui:build` 会生成 Dashboard 控制面板所需的前端资源：

- `dist/control-ui/index.html` — Dashboard 入口页面
- `dist/control-ui/assets/*.js` — Dashboard 前端 JS 资源
- `dist/control-ui/assets/*.css` — Dashboard 样式资源

> ⚠️ **踩坑记录 1（2026.4.8 / 2026.4.9）**：跳过了 `pnpm build`，导致 `dist/entry.js` 不存在。
> 用户安装后运行报错：`Error: stableclaw: missing dist/entry.(m)js (build output).`
>
> ⚠️ **踩坑记录 2（2026.4.10）**：跑了 `pnpm build` 但没跑 `pnpm ui:build`，
> 导致 `dist/control-ui/` 目录缺失。用户安装后打开 Dashboard 报错：
> `Control UI assets not found. Build them with pnpm ui:build.`
> **这两条命令都必须跑！**

### 步骤 4：验证构建产物

构建完成后，**必须确认以下文件全部存在**（任何一项缺失都说明构建不完整，不能发布）：

```bash
# === 核心（缺一不可，否则启动直接崩溃）===
ls dist/entry.js
ls dist/index.js

# === Dashboard UI（缺失则打开 Dashboard 报错）===
ls dist/control-ui/index.html

# === 扩展运行时接口 ===
ls dist/extensions/speech-core/runtime-api.js
ls dist/extensions/image-generation-core/runtime-api.js
ls dist/extensions/media-understanding-core/runtime-api.js

# === 插件 SDK 导出 ===
ls dist/plugin-sdk/core.js
ls dist/plugin-sdk/compat.js
```

**特别注意**：如果 `dist/control-ui/index.html` 不存在，说明忘了跑 `pnpm ui:build`，
必须补跑后再发布。

也可以运行项目自带的发布检查脚本（会自动检查上述文件）：

```bash
node --import tsx scripts/release-check.ts
```

这个脚本会自动检查包内容、版本号、扩展清单、包大小等。

### 步骤 5：Dry-run 打包测试

```bash
npm pack --dry-run
```

确认打包过程没有报错，且输出中包含 `dist/entry.js`。

### 步骤 6：配置 npm 认证并发布

```bash
# 写入 token 到 .npmrc（发布后会自动清理）
echo "//registry.npmjs.org/:_authToken=<你的npm_token>" >> .npmrc

# 发布
npm publish --access public
```

或者使用一键发布脚本：

```bash
./scripts/publish-npm.sh <npm_token>
```

### 步骤 7：发布后验证

```bash
# 确认 registry 上版本正确
npm view stableclaw version

# 全局安装测试
npm install -g stableclaw@latest
stableclaw --version
```

### 步骤 8：推送代码到 Git

```bash
git add package.json
git commit -m "chore: bump version to <新版本号>"
git push origin main
```

## 三、踩坑记录

### 坑 1：prepublishOnly 未包含 UI 构建（2026.4.20 修复）

**现象**：使用 `npm publish` 发布时，虽然 `pnpm build` 自动执行了，但 Dashboard UI
没有构建。用户安装后打开 Dashboard 报错：
```
Control UI assets not found. Build them with `pnpm ui:build` (auto-installs UI deps),
or run `pnpm ui:dev` during development.
```

**原因**：`package.json` 中的 `prepublishOnly` 脚本只写了 `pnpm build`，没有包含
`pnpm ui:build`。虽然发布文档反复强调要跑两条命令，但 `npm publish` 的自动构建
流程只跑了后端编译，UI 构建被遗漏。

**修复**：将 `prepublishOnly` 改为：
```json
"prepublishOnly": "pnpm build && pnpm ui:build"
```
这样 `npm publish` 时会自动完成完整构建（后端 + UI），不会再遗漏。

**教训**：虽然文档写得再清楚，自动化流程才是最可靠的。确保 `prepublishOnly`
包含所有必需的构建步骤。

### 坑 6：使用 `--ignore-scripts` 发布导致缺少 UI 资源（2026.5.2）

**现象**：发布的包安装后打开 Dashboard 报错：
```
Control UI assets not found. Build them with `pnpm ui:build`.
```
但 `dist/` 目录里确实有 `control-ui/`，发布前也验证过。

**原因**：使用了 `npm publish --ignore-scripts`。这个参数会跳过 `prepublishOnly`，
虽然跳过了构建（节省时间），但如果在构建之后、发布之前有任何操作清空了 `dist/`
（比如重新跑了 `pnpm build` 而 tsdown 清空了 `dist/control-ui/`），或者构建顺序
不对，包里就不会有 UI 产物。

**更严重的场景**：`prepublishOnly` 中的 `pnpm build` 会重新编译整个 `dist/` 目录，
然后 `pnpm ui:build` 才写入 `dist/control-ui/`。如果用 `--ignore-scripts` 跳过了
这个流程，而本地的 `dist/control-ui/` 被之前的构建清掉了，发布的包就是残缺的。

**规则**：
1. **永远不要用 `npm publish --ignore-scripts`**，除非你100%确定本地的 `dist/` 包含
   完整的构建产物（包括 `dist/control-ui/index.html`）。
2. 如果必须跳过构建（如 CI 环境已预构建），发布前**必须**执行验证：
   ```bash
   ls dist/control-ui/index.html  # 必须存在！
   npm pack --dry-run 2>&1 | rg "control-ui"  # 必须输出文件列表！
   ```

### 坑 7：`scripts/ui.js` 不支持 npm/npx 环境（2026.5.3 修复）

**现象**：在没有安装 pnpm 的环境中执行 `node scripts/ui.js build` 报错：
```
Missing UI runner: install pnpm, then retry.
```
导致 `prepublishOnly` 中的 `pnpm ui:build` 失败。

**原因**：`scripts/ui.js` 的 `resolveRunner()` 函数只查找 `pnpm`，找不到就直接退出。
但很多开发者和 CI 环境只有 npm/npx，没有 pnpm。

**修复**：让 `resolveRunner()` 支持 npx 作为 fallback。当 pnpm 不可用时，
自动回退到 `npx vite build`。

**教训**：`prepublishOnly` 中的所有步骤都必须在尽可能多的环境中可靠运行。
不应该强制要求某个特定的包管理器。

### 坑 2：漏跑 UI 构建（2026.4.10）

**现象**：用户 `npm install -g stableclaw@latest` 后，打开 Dashboard 报错：
```
Control UI assets not found. Build them with `pnpm ui:build` (auto-installs UI deps),
or run `pnpm ui:dev` during development.
```

**原因**：`pnpm build` 和 `pnpm ui:build` 是两条**独立**的命令。
`pnpm build` 只负责 TypeScript 编译（tsdown），不会构建 Dashboard 前端资源。
如果发布前只跑了 `pnpm build` 而没跑 `pnpm ui:build`，
包里就不会有 `dist/control-ui/` 目录，Dashboard 无法加载。

**教训**：发布前必须跑**两条**命令：
```bash
pnpm build      # 后端/核心编译
pnpm ui:build   # Dashboard 前端构建
```
漏掉任何一条都会导致用户端功能异常。

### 坑 2：未构建直接发布（2026.4.8 / 2026.4.9）

**现象**：用户 `npm install -g stableclaw@latest` 后，运行 `stableclaw` 命令报错：
```
Error: stableclaw: missing dist/entry.(m)js (build output).
```

**原因**：发布前没有执行 `pnpm build`，`dist/` 目录中缺少 `entry.js`。
`stableclaw.mjs`（CLI 启动脚本）在第 173-178 行会尝试导入 `./dist/entry.js`，
找不到时抛出这个致命错误，整个程序无法启动。

**教训**：`dist/` 不在 git 版本控制中，它只存在于本地构建后。发布时 npm 打包的是
本地文件系统中的文件，不会自动帮你构建。**永远先构建再发布。**

### 坑 3：pnpm 不在 PATH 中

**现象**：`pnpm build` 命令报错 `Command "build" not found`，或 `scripts/tsdown-build.mjs`
执行后 `dist/` 目录没有任何变化。

**原因**：在某些环境中（如仅有 corepack 没有 pnpm 全局安装），
`pnpm` 命令不在 PATH 里。构建脚本内部调用 `pnpm exec tsdown` 时静默失败。

**解决**：确保 `pnpm` 可用：
```bash
# 方法 1：corepack 自动安装
corepack enable

# 方法 2：npm 全局安装
npm install -g pnpm

# 方法 3：手动加入 corepack shims 到 PATH
export PATH="/usr/lib/node_modules/corepack/shims:$PATH"
```

### 坑 4：旧 dist 残留混淆

**现象**：构建后发布的包运行异常，某些文件是旧版本的。

**原因**：上一次构建的 `dist/` 文件残留，和新构建混合在一起。

**解决**：构建前先清理旧产物：
```bash
rm -rf dist
pnpm build
```

### 坑 5：版本号冲突

**现象**：`npm publish` 报错 `403 Forbidden` 或 `version already published`。

**原因**：`package.json` 中的版本号在 npm registry 上已经存在。

**解决**：先查看已有版本，再 bump：
```bash
npm view stableclaw versions --json
# 修改 package.json 中的 version 为更高版本
```

## 四、一键发布脚本

项目提供了 `scripts/publish-npm.sh`，集成了以上所有检查步骤：

```bash
# 标准用法（自动：检查环境 → 构建 → 验证产物 → 发布）
./scripts/publish-npm.sh <npm_token>

# 通过环境变量传入 token
NPM_TOKEN=npm_xxxxx ./scripts/publish-npm.sh

# 跳过构建（仅当 dist/ 已存在时使用）
./scripts/publish-npm.sh --skip-build <npm_token>
```

脚本内置了以下安全机制：
- **构建产物验证**：检查 `dist/entry.js`、`dist/control-ui/index.html`、`dist/index.js` 等关键文件
- **npm pack 预检**：发布前执行 dry-run 确保包能正常打包
- **corepack shim 回退**：自动检测 pnpm 可用性
- **旧 dist 清理**：构建前自动清理旧的 `dist/` 目录

## 五、构建命令详情

`pnpm build` 实际执行的是一条很长的构建链：

```
pnpm canvas:a2ui:bundle           # Canvas A2UI 资源打包
node scripts/tsdown-build.mjs     # 核心 TypeScript 编译（tsdown）
node scripts/runtime-postbuild.mjs # 运行时后处理
node scripts/build-stamp.mjs      # 写入构建时间戳
pnpm build:plugin-sdk:dts         # 插件 SDK 类型声明
node scripts/write-plugin-sdk-entry-dts.ts
node scripts/check-plugin-sdk-exports.mjs
node scripts/canvas-a2ui-copy.ts
node scripts/copy-hook-metadata.ts
node scripts/copy-export-html-templates.ts
node scripts/write-build-info.ts
node scripts/write-cli-startup-metadata.ts  # CLI 启动元数据
node scripts/write-cli-compat.ts
```

其中最核心的是 `tsdown-build.mjs`，它负责编译 `src/entry.ts` → `dist/entry.js`。

**但注意：以上构建链不包含 Dashboard UI！** UI 构建是独立的：
```
pnpm ui:build   # 等同于 node scripts/ui.js build
```

如果手动分步构建，至少要执行：
```bash
# 后端核心（pnpm build 的子集）
node scripts/tsdown-build.mjs
node scripts/runtime-postbuild.mjs
node scripts/build-stamp.mjs

# Dashboard UI（必须单独执行！）
pnpm ui:build
```

## 六、真实部署测试验证清单（2026.5.3 实测通过）

以下是在本地真实部署 StableClaw 并通过 Dashboard 聊天功能测试的完整记录，供后续发布参考。

### 测试环境

- OS: Linux (Ubuntu)
- Node.js: v24.14.1
- StableClaw version: 2026.5.3
- 构建模式: full gateway (`gateway run --full`)

### 测试步骤与结果

#### 1. 构建产物验证 ✅

```bash
ls dist/entry.js                    # ✅ 存在
ls dist/index.js                    # ✅ 存在
ls dist/control-ui/index.html        # ✅ 存在
ls dist/control-ui/assets/*.js      # ✅ 存在 (76+ 文件)
```

#### 2. 配置文件格式

配置文件位于 `~/.stableclaw/stableclaw.json`（JSON5 格式）。

**关键配置字段注意**：
- `gateway.mode` 必须是 `"local"` 或 `"remote"`（不能是 `"lite"`，`"lite"` 不是有效值）
- `gateway.bind` 必须使用模式名（`"lan"`, `"loopback"`, `"auto"` 等），不能直接写 IP 地址如 `"0.0.0.0"`
- `meta.version` 不是有效字段，会导致配置验证失败

正确示例：
```json
{
  "gateway": {
    "mode": "local",
    "port": 18789,
    "bind": "lan",
    "auth": { "mode": "token", "token": "your-token" }
  },
  "models": {
    "mode": "merge",
    "providers": { ... }
  }
}
```

#### 3. 启动模式选择

- **Lite 模式**（默认 `gateway run`）：启动快（~7ms），但**不支持 chat 方法**。
  聊天方法会返回：`method "chat.send" requires the full gateway runtime`。
- **Full 模式**（`gateway run --full`）：启动较慢（~5-10s），但支持所有功能包括聊天。

**Dashboard 聊天必须使用 full 模式启动。**

#### 4. Dashboard 连接

Dashboard 通过 WebSocket 连接 gateway，使用以下协议格式：

```json
{
  "type": "req",
  "id": "1",
  "method": "connect",
  "params": {
    "minProtocol": 1,
    "maxProtocol": 3,
    "auth": { "token": "your-token" },
    "client": {
      "id": "openclaw-control-ui",
      "version": "2026.5.3",
      "platform": "web",
      "mode": "ui"
    }
  }
}
```

注意 `client` 字段有严格的 schema 要求：
- `id`: 必须是预定义值之一（如 `openclaw-control-ui`, `cli`, `test` 等）
- `version`: 必填字符串
- `platform`: 必填字符串
- `mode`: 必填（如 `"ui"`, `"cli"`, `"test"` 等）

#### 5. 模型配置验证

使用 ModelScope 的 `stepfun-ai/Step-3.5-Flash` 模型测试聊天：
- ✅ 消息成功发送到模型
- ✅ 模型返回完整中文回复
- ✅ Dashboard 正确显示对话历史
- ✅ Token 使用量显示正常（↑23.2k, ↓285, 12% ctx）

#### 6. Origin 限制

WebSocket 连接有 origin 校验。如果浏览器不是从 gateway 地址直接打开（例如通过代理或不同域名），会报错：
```
origin not allowed (open the Control UI from the gateway host or allow it in gateway.controlUi.allowedOrigins)
```

连续多次认证失败会触发限流：
```
unauthorized: too many failed authentication attempts (retry later)
```

解决方案：
1. 直接从 gateway 提供的地址访问 Dashboard
2. 或在配置中添加 `gateway.controlUi.allowedOrigins`

### 踩坑 8：配置验证导致 Gateway 拒绝启动（2026.5.3 修复）

**现象**：配置文件中写了无效字段，Gateway 拒绝启动并将配置保存为 `.error` 文件。

**原因**：配置 schema 验证非常严格。以下字段/值都会导致验证失败：
- `meta.version` — 不识别的键
- `gateway.mode: "lite"` — 不在枚举值中（只允许 `"local"` 和 `"remote"`）
- `gateway.bind: "0.0.0.0"` — IP 地址别名已废弃，必须用模式名

**教训**：修改配置前最好先用 `node stableclaw.mjs doctor` 检查，或查阅 config schema。

### 踩坑 9：npm publish 超时但实际发布成功（2026.5.7）

**现象**：`npm publish` 命令在 CLI 端超时报错，但实际上版本已经成功发布到 npm registry。

**原因**：大体积 npm 包（~9MB tarball，3251 个文件）上传过程中，CLI 的 HTTP 请求超时。
但 npm registry 端可能在超时前已收到完整数据并完成发布。

**解决**：
1. 发布后用 `npm view stableclaw version` 验证版本号
2. 如果版本已存在，说明之前发布成功，不需要重试
3. 对于大包可考虑使用 `npm pack` + 手动上传，或增加网络超时时间

**发布记录 2026.5.7**：
- Onboard UI 完整向导（12步）：安全确认、流程选择、增强Provider选择（9家）、模型选择、工作空间配置、搜索配置、技能配置、Hooks配置、完成摘要
- 修复 Gateway Lite 模块路由：exec 方法前缀修正、新增 14 个缺失方法前缀、plugin 模块映射修正、removeChatRun 参数补全
- 聊天功能审查确认：WebSocket RPC AJAX 模式正确实现，无需修改
- 新增 API 端点：/api/onboard/skills、/api/onboard/hooks、/api/onboard/search-config、/api/onboard/skills-config、/api/onboard/hooks-config

### 踩坑 10：Lite 模式 HTTP 健康检查端点无响应（2026.4.7 发现）

**现象**：使用 `gateway run`（不带 `--full`，即 Lite 模式）启动后，`/healthz` 和 `/readyz` 端点无响应，`/ready` 返回 `{"error":"not found"}`。

**原因**：Lite 模式声称 "gateway kernel ready in 12ms" 但 HTTP 健康检查路由未正确注册。Full 模式下所有端点正常。

**影响**：使用 Lite 模式部署的用户无法通过健康检查探针监控网关状态。Docker/K8s 的 liveness/readiness probe 会失败。

**临时解决**：使用 `--full` 模式启动。

### 踩坑 11：model-pricing 模块启动超时（2026.4.7 发现）

**现象**：Full 模式启动后约 40 秒，日志输出 `model-pricing pricing bootstrap failed: TimeoutError`。

**原因**：`model-pricing` 模块在启动时尝试从外部获取模型定价数据，但请求超时。

**影响**：非致命错误，网关继续运行，但模型定价信息加载失败可能影响用量统计和计费功能。

### 踩坑 12：日志前缀截断（2026.4.7 发现）

**现象**：多个子系统日志输出中前缀字符丢失，如 `[eartbeat]`、`[ooks]`、`[odel-pricing]`。

**原因**：推测是异步日志格式化与 stdout 输出的竞争条件导致缓冲区截断。

**影响**：日志可读性下降，但不影响功能。影响日志分析和问题排查效率。

### 踩坑 13：Gateway 进程在 Shell 会话结束后终止（2026.4.7 发现）

**现象**：通过 `nohup node stableclaw.mjs gateway run &` 启动的网关，在父 Shell 退出后也被终止。

**原因**：Gateway 内部的进程 fork 机制导致子进程依赖父进程的生命周期。

**解决**：生产环境应使用 `stableclaw gateway start`（systemd/launchd）来持久化运行，而非直接在后台运行。
